(let ([proc
       (block
        (raise "Wooot")
        (= :foo (read))
        (print 42)
        (print (/ 10 5))
        (? :foo)
        )])
  (proc))


;; TYPE:
;;
;;   Int
;;
;;   < exception : String
;;   , io
;;   , state : { foo : Int | μ102 }
;;   , partial | μ101
;;   >
